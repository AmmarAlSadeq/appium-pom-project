package org.automation.locators;

/**
 * Locator constants for the ScrollView > 2. Long screen.
 * Contains ONLY selector strings — no logic, no methods.
 */
public class ScrollViewLocators {

    public static final String LONG_SCROLLVIEW = "2. Long";
}
